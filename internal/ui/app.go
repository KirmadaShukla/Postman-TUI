package ui

// App entry types live in model.go (New / Init / Model).
// This file remains as the package's conventional entry name so editors
// that still reference app.go after the split do not keep a deleted buffer
// that redeclares helpers now owned by format.go and related files.
